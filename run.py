from social_force import run_code_depending_on_part as run_code

# run 
if False: run_code(part="number_obs", filename="noname", N_skip_animation=1000);
if True: run_code(part="design_choice", filename="noname", N_skip_animation=1);
if False: run_code(part="std_of_speed", filename="noname", N_skip_animation=1000);